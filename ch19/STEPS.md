# ch19 文件 IO 步骤卡（了解级，9/24–26）

> 定位：M1 收尾模块之一，也是简历项目「V4L2 采集存 jpg」的直接地基——摄像头每帧都要 fwrite 进文件。了解级：会用文本/二进制读写、养成 fclose 配对习惯即可，不深挖 ftell/fseek/高级格式化（留 M3 系统编程）。
> 用法：照步骤自己写，跑通对照「验收输出」，卡住发我 review。递减骨架，不给完整代码。
> 三天节奏：9/24 文本读写 → 9/25 二进制+结构体存档 → 9/26 综合小项目（缝合 ch13/ch15）。

---

## 核心概念先记牢
- `FILE* fp = fopen("路径", "模式");` —— FILE 是结构体，fp 是句柄指针；**失败返回 NULL，必须判空**。
- 模式：`"r"` 读(文件须存在) / `"w"` 写(不存在则建，**存在则清空**) / `"a"` 追加 / 加 `"b"` 表二进制(rb/wb/ab) / 加 `"+"` 表读写。
- 用完 `fclose(fp);` —— **必须配对**，否则缓冲不刷新、句柄泄漏。
- 文本 vs 二进制：Windows 文本模式 `\n`↔`\r\n` 自动转换；二进制用 `"b"` 原样读写。
- 统一开头：`if(fp==NULL){ perror("fopen"); return 1; }`。

---

## Day 1（9/24）文本文件读写

### 练习1 写 + 读文本（必做）
**写什么**
- 用 `fopen("test.txt","w")` → `fprintf`/`fputs` 写几行（如学生姓名/成绩）→ `fclose`。
- 再用 `fopen("test.txt","r")` → `fgets` 逐行读到缓冲区 → 打印 → `fclose`。

**验收输出**
- 读出来的内容 == 写进去的内容。

**坑预警**
- 写用 `"w"` 会**清空**原文件；读用 `"r"` 文件不存在则 fopen 返 NULL（必须判空，别解引用空指针）。
- `fgets(buf, sizeof(buf), fp)` 读不到时返 NULL，用它控制循环。

### 练习2 统计（必做）
**写什么**
- `fopen` 一个已知文本，`fgetc` 逐字符读，统计字符数 / 行数（遇 `\n` +1）。

**验收输出**
- 对一个已知文本，手算行数/字符数核对一致。

**坑预警**
- 别用 `feof()` 控制循环（会多读一次）；用 `while((c=fgetc(fp))!=EOF)`。
- `fgetc` 返回 `int`（要能装下 EOF），别用 `char` 接。

### 练习3 复制文件（可选）
**写什么**
- `fopen` 两个句柄（src `"rb"`, dst `"wb"`），逐块 `fread`/逐字符 `fgetc` 写入 dst。

**坑预警**
- 两个 `FILE*` 别搞混；最后都 `fclose`。

---

## Day 2（9/25）二进制 + 结构体存档

### 练习4 结构体写入文件（必做）
**写什么**
- 定义 `struct Student{int id; char name[20]; float score;}`。
- `fopen("stu.dat","wb")` → `fwrite(&s, sizeof(s), 1, fp)` → `fclose`。
- 再 `"rb"` → `fread(&s, sizeof(s), 1, fp)` 读回 → 打印核对。

**验收输出**
- 读回的 id/name/score 与写入一致。

**坑预警**
- 二进制**必须用 `"b"`**；`fwrite` 返回成功写入的元素个数，应检查 `==1`。

### 练习5 批量存档（必做）
**写什么**
- `struct Student arr[N]`，循环 `fwrite` 多个 → 读回循环 `fread`，验证顺序一致。

---

## Day 3（9/26）综合小项目（缝合 ch13/ch15）

### 练习6 简易成绩/通讯录存档
**写什么**
- 输入若干学生（struct + 枚举状态），写文件存档；程序重启后读文件还原列表。
- 重点：把 ch13 结构体 + ch15 枚举 + 今天文件 IO 串起来，做一个能「存/取」的小工具。

---

## 闭眼复现（练完不翻代码默写）
1. `fopen` 常见模式 `r/w/a` 各自行为（尤其 `"w"` 清空）
2. 为什么必须 `fclose`
3. 文本模式 vs 二进制模式区别（何时加 `"b"`）

---

## git 提交（按天或模块末）
```powershell
git add ch19/ && git commit -m "ch19 文件IO：文本读写/二进制/结构体存档" && git push && git push origin main
```
